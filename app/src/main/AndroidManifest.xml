<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.thegiant.mylibraryproject">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/trolly"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/trolly"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".ForgetPassword"
            android:theme="@style/Theme.AppCompat.Light.Dialog.Alert"></activity>
        <activity android:name=".SubListActivity" />
        <activity android:name=".MainActivity" />
        <activity
            android:name=".WelcomeActivity"
            android:label="@string/app_name"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".SemChoiceActivity"
            android:label="Semesters"
            android:theme="@style/AppTheme" />
    </application>

</manifest>